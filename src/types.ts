import { LucideIcon } from "lucide-react";

export interface ServiceGroup {
  icon: LucideIcon;
  title: string;
  desc: string;
}

export interface Feature {
  icon: LucideIcon;
  title: string;
  description: string;
}
